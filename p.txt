Принцып работы:
#1 Конструктор:
	Экземпляр module_loader
	Поиск конфига:
		Есть:
			Сохраняем путь до конфига
			Берём конфиг
			Если (пустой){
				Заполняем:
					(new Config)->create_start_config($this, 'use');
			} иначе {
				Обрабатываем:
					Получаем содержимое,
					И подгружаем настройки модулей:
						$this->module_loader->update_modules_config($config['modules']);
			}
		Нету:
			Выводим сообщение и логи что Нету
	Рендерим код:
		$this->renderCode();

#2 renderCode:
	Получаем содержимое файла
	Получаем настройки модулей
	Перебираем (модули как модуль){
		Получаем/подключаем модуль as $module
		({$module = require $file;})
		Получаем имя модуля
		Получаем тип модуля
		if (count(настройки модулей) > 0){
			// Устанавливаем настройки из конфига для модулей
			$sets = $this->setConfigSettings($module);
		}
	}